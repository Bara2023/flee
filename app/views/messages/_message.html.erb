<div id="message-<%= message.id %>">
  <small>
    <strong><%= message.user.name %></strong>
    <i><%= message.created_at.strftime("%A %e %B  à %H:%M") %></i>
  </small>
  <p><%= message.content %></p>
</div>
